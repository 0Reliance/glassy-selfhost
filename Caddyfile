# Caddy reverse proxy for the Glassy self-hosted appliance.
# GLASSY_DOMAIN is injected from .env by docker-compose.https.yml.
# Caddy provisions and renews a Let's Encrypt certificate automatically.
{$GLASSY_DOMAIN} {
	encode zstd gzip
	reverse_proxy glassy:8080
}
